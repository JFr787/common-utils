[project]
name = "common-utils"
version = "0.1.0"
description = "Common Python utilities"
authors = [
  { name = "JF" }
]
requires-python = ">=3.11"
readme = "README.md"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"